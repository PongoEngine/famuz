let r1 = @1/4 9--- 4---
let steps = [0,1]
func cool r i = ${
  duration: r.duration, 
  velocity: r.velocity
  step: (r.velocity == -1) ? 0 : 10
}

func main = {
    let q = mapi(r1 cool)
    print(q)
}